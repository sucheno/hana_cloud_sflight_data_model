-- generated by cds-compiler version 1.26.2
COLUMN TABLE teched_flight_trip_Airlines (
  IATA2 NVARCHAR(2) NOT NULL,
  Name NVARCHAR(100),
  Country NVARCHAR(50),
  PRIMARY KEY(IATA2)
) WITH ASSOCIATIONS (
  MANY TO MANY JOIN teched_flight_trip_EarthRoutes AS Routes ON (Routes.Airline_IATA2 = IATA2)
)